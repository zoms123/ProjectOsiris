using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class BasicShadowAttackStrategy<T> : IAttackStrategy where T : MonoBehaviour
{
    public void Execute()
    {
        
    }

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
